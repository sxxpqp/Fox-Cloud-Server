# fox-cloud-server-repository-comp-files

## 相关介绍

这是文件类型的组件，特点是以文件为形态。

包括，服务组件、设备模板组件、前端界面组件、JAR解码器组件，也包括初始化安装包。

这些组件，都是以文件形态存在的，所以涉及到文件的上传、下载、文件版本等跟文件相关的形式。

## 区别

跟隔壁的fox-cloud-server-repository-comp-script的区别，script是脚本类型的组件

它们体现为一个个脚本和配置信息，而不是一个个文件。

文件型组件，体现为静态的特点，而脚本型组件体现为动态的特点。


